# AGENTS Notes

- 本机可直接使用 `scoop` 命令（PowerShell 环境已可用）。
- 每次修改完成后都要同步到 GitHub（至少提交并推送对应分支）。
- 如果是重大修改，或小修改累计多次后，要同步更新应用版本号，并同时更新 GitHub Release 与应用内版本信息/更新日志。
- 每次准备提交或发布前，必须先在本地完成打包验证（至少先执行 Windows 打包），打包通过后再进行后续 GitHub 同步与发布动作。
